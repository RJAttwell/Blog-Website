<%- include("partials/header") -%>

    <h1>Home</h1>

    <br>


    <!-- Upper Bound is the length of our posts array -->
    <% //for(var i=0; i < posts.length; i++ ) {%>

        <!-- <p> -->
            <%= //posts[i] %>
        <!-- </p> -->
        <!-- Log just the title of the posts array -->
        <!-- <% //console.log(posts[i].title); %> -->
    <%//}%>

    <!-- Instead of a for loop, we can use the forEach method -->

    <% posts.forEach(function(post){ %>
        <h2><%=post.title%></h2>
        <p><%=post.body%></p>
        
    <%});%>


<%- include("partials/footer") -%>